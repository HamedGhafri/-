# دليل التثبيت والتحديث 🚀

هذا الدليل يشرح كيفية رفع الملفات المحسّنة إلى مستودع GitHub الخاص بك.

## 📋 المتطلبات

- حساب GitHub نشط
- Git مثبت على جهازك
- محرر نصوص (VS Code, Notepad++, إلخ)

## 🔄 خطوات التحديث

### الطريقة 1: عبر GitHub Web Interface (الأسهل)

#### 1. حذف الملفات القديمة
1. اذهب إلى مستودعك: `https://github.com/HamedGhafri/-`
2. اختر ملف `html_template` (إذا كان موجوداً)
3. اضغط على أيقونة سلة المهملات لحذفه
4. اكتب سبب الحذف: "حذف الملفات غير المستخدمة"
5. اضغط "Commit changes"

#### 2. تحديث الملفات الموجودة
لكل ملف من الملفات التالية:
- `index.html`
- `about.html`
- `viewer.html`
- `style.css`
- `script.js`

اتبع الخطوات:
1. افتح الملف في المستودع
2. اضغط على أيقونة القلم (Edit)
3. احذف المحتوى القديم
4. الصق المحتوى الجديد من الملفات المحسّنة
5. اكتب وصف التحديث: "تحديث [اسم الملف] - نسخة محسّنة"
6. اضغط "Commit changes"

#### 3. إضافة الملفات الجديدة
للملفات الجديدة:
- `categories.html`
- `search.html`
- `favorites.html`
- `404.html`
- `manifest.json`
- `sitemap.xml`
- `INSTALLATION.md`

اتبع الخطوات:
1. اذهب للصفحة الرئيسية للمستودع
2. اضغط "Add file" > "Create new file"
3. اكتب اسم الملف (مثلاً: `categories.html`)
4. الصق محتوى الملف
5. اضغط "Commit new file"

#### 4. تحديث README.md
1. افتح ملف `README.md`
2. اضغط Edit
3. استبدل المحتوى بالنسخة الجديدة
4. احفظ التغييرات

### الطريقة 2: عبر Git Command Line (للمتقدمين)

```bash
# 1. استنساخ المستودع
git clone https://github.com/HamedGhafri/-
cd -

# 2. حذف الملف غير المستخدم
git rm html_template
git commit -m "حذف html_template"

# 3. نسخ جميع الملفات المحسّنة إلى المجلد
# (انسخ الملفات يدوياً من مجلد diwan إلى مجلد المستودع)

# 4. إضافة جميع التغييرات
git add .

# 5. حفظ التغييرات
git commit -m "تحديث شامل للموقع - نسخة محسّنة 2.0"

# 6. رفع التغييرات
git push origin main
```

### الطريقة 3: عبر GitHub Desktop (موصى بها)

1. حمّل وثبّت [GitHub Desktop](https://desktop.github.com/)
2. افتح البرنامج وسجّل دخول
3. اضغط "Clone Repository"
4. اختر `HamedGhafri/-`
5. انسخ الملفات المحسّنة إلى المجلد
6. ستظهر التغييرات في البرنامج تلقائياً
7. اكتب وصف التحديث في الأسفل
8. اضغط "Commit to main"
9. اضغط "Push origin"

## ✅ التحقق من التحديث

بعد رفع الملفات:

1. انتظر 1-2 دقيقة لبناء الموقع
2. افتح: `https://hamedghafri.github.io/-/index.html`
3. اضغط Ctrl+F5 (أو Cmd+Shift+R على Mac) لتحديث الصفحة
4. تحقق من:
   - ✅ التصميم الجديد يعمل
   - ✅ القوائم تعمل بشكل صحيح
   - ✅ البحث يعمل
   - ✅ المفضلة تعمل
   - ✅ الوضع الليلي/النهاري يعمل

## 🔧 حل المشاكل الشائعة

### المشكلة: الموقع لا يظهر التحديثات
**الحل:**
1. امسح ذاكرة التخزين المؤقت للمتصفح (Ctrl+Shift+Delete)
2. جرب متصفح آخر
3. تأكد من أن GitHub Pages مفعّل في إعدادات المستودع

### المشكلة: خطأ 404 عند فتح الموقع
**الحل:**
1. تحقق من أن GitHub Pages مفعّل
2. تأكد من أن الرابط صحيح
3. انتظر بضع دقائق لبناء الموقع

### المشكلة: الملفات لم تُرفع بشكل صحيح
**الحل:**
1. تحقق من أن جميع الملفات موجودة في المستودع
2. تأكد من أن أسماء الملفات صحيحة
3. تأكد من عدم وجود أخطاء في console المتصفح (F12)

## 📝 قائمة التحقق النهائية

قبل الانتهاء، تأكد من:

- [ ] تم حذف `html_template`
- [ ] تم تحديث جميع الملفات القديمة
- [ ] تم إضافة جميع الملفات الجديدة
- [ ] تم تحديث `README.md`
- [ ] تم تحديث `robots.txt`
- [ ] تم تحديث `sitemap.xml`
- [ ] الموقع يعمل بشكل صحيح
- [ ] جميع الروابط تعمل
- [ ] الصور والأيقونات تظهر

## 🎉 تهانينا!

موقعك الآن محدّث ويعمل بأحدث نسخة محسّنة!

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع هذا الدليل مرة أخرى
2. تحقق من GitHub Issues
3. ابحث عن الخطأ في Google
4. اطلب المساعدة من مجتمع المطورين

---

**بالتوفيق! 🚀**
