# 📦 دليل التثبيت - ديوان حمد الغافري

## 🚀 طرق التثبيت

### الطريقة الأولى: رفع على GitHub Pages (موصى بها)

#### الخطوات:

1. **إنشاء مستودع GitHub جديد**
   ```
   اذهب إلى github.com
   اضغط على "New repository"
   أدخل اسم المستودع مثل: "diwan"
   اختر Public
   اضغط "Create repository"
   ```

2. **رفع الملفات**
   
   **الطريقة أ: عبر واجهة GitHub**
   ```
   اضغط "uploading an existing file"
   اسحب جميع الملفات أو اختر الملفات
   اضغط "Commit changes"
   ```

   **الطريقة ب: عبر Git (إذا كان مثبتاً)**
   ```bash
   # في مجلد الملفات
   git init
   git add .
   git commit -m "النسخة الأولى من الديوان"
   git branch -M main
   git remote add origin https://github.com/username/diwan.git
   git push -u origin main
   ```

3. **تفعيل GitHub Pages**
   ```
   اذهب لإعدادات المستودع (Settings)
   اضغط على "Pages" من القائمة الجانبية
   في قسم "Source" اختر "main" branch
   اختر المجلد "/" (root)
   اضغط "Save"
   انتظر 1-2 دقيقة
   الموقع سيكون جاهزاً على:
   https://username.github.io/diwan/
   ```

4. **تعديل الروابط (مهم جداً)**
   - افتح ملف `sitemap.xml`
   - غيّر `hamedghafri.github.io/-/` إلى رابطك الجديد
   - افتح ملف `robots.txt`
   - غيّر رابط Sitemap إلى رابطك الجديد

---

### الطريقة الثانية: استضافة على سيرفر خاص

#### متطلبات:
- استضافة ويب (Hosting)
- FTP أو File Manager

#### الخطوات:
1. ضغط جميع الملفات في ملف ZIP
2. رفع الملف عبر FTP أو File Manager
3. فك الضغط في المجلد الرئيسي (public_html)
4. الموقع سيكون جاهزاً على نطاقك

---

### الطريقة الثالثة: تشغيل محلي للاختبار

#### باستخدام Python:
```bash
# افتح Terminal/CMD في مجلد الملفات
cd path/to/diwan

# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# افتح المتصفح على
http://localhost:8000
```

#### باستخدام Node.js:
```bash
# تثبيت serve (مرة واحدة)
npm install -g serve

# تشغيل السيرفر
cd path/to/diwan
serve

# أو
npx serve

# افتح المتصفح على
http://localhost:3000
```

#### باستخدام PHP:
```bash
cd path/to/diwan
php -S localhost:8000

# افتح المتصفح على
http://localhost:8000
```

#### باستخدام Live Server في VS Code:
```
1. ثبّت إضافة "Live Server" في VS Code
2. افتح مجلد المشروع في VS Code
3. اضغط بزر الماوس الأيمن على index.html
4. اختر "Open with Live Server"
```

---

## 📝 إعداد ملف القصائد

### تنسيق ملف poems.txt:

```text
### عنوان القصيدة الأولى

هذا هو البيت الأول من القصيدة
وهذا هو البيت الثاني من القصيدة
والبيت الثالث يأتي هنا
البيت الرابع والأخير

### عنوان القصيدة الثانية

البيت الأول من القصيدة الثانية
البيت الثاني من القصيدة الثانية
```

### قواعد مهمة:
- ✅ استخدم `###` (ثلاث علامات) قبل عنوان كل قصيدة
- ✅ اترك سطراً فارغاً قبل وبعد كل قصيدة
- ✅ كل سطر = بيت شعري واحد
- ✅ لا تضع أرقاماً أو رموزاً قبل الأبيات
- ❌ لا تستخدم `#` أو `##` فقط
- ❌ لا تترك أسطر فارغة داخل القصيدة نفسها

---

## 🎨 التخصيص

### تغيير الألوان:
افتح ملف `style.css` وعدّل القسم التالي:

```css
:root {
    /* غيّر هذه القيم */
    --primary-color: #8b6914;      /* اللون الأساسي */
    --primary-dark: #6b4f0a;       /* اللون الداكن */
    --secondary-color: #d4af37;    /* اللون الثانوي */
}
```

### تغيير الخطوط:
في ملف `style.css`:

```css
:root {
    --font-primary: 'Amiri', serif;        /* للنصوص الشعرية */
    --font-secondary: 'Cairo', sans-serif;  /* للعناوين والقوائم */
}
```

### إضافة شعار أو صورة:
1. ضع ملف الصورة في المجلد الرئيسي
2. سمّه `logo.png` أو `poet-image.jpg`
3. أضف الكود التالي في `about.html`:

```html
<div class="profile-avatar">
    <img src="poet-image.jpg" alt="حمد الغافري">
</div>
```

---

## 🔧 الصيانة

### إضافة قصائد جديدة:
1. افتح ملف `poems.txt`
2. أضف القصيدة الجديدة في النهاية
3. احفظ الملف
4. ارفعه على GitHub أو السيرفر

### تحديث معلومات الشاعر:
1. افتح `about.html`
2. عدّل النصوص في القسم `<section class="bio-section">`
3. احفظ وارفع الملف

### النسخ الاحتياطي:
```bash
# انسخ جميع الملفات لمكان آمن
# أو استخدم Git
git add .
git commit -m "نسخة احتياطية - $(date)"
git push
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: القصائد لا تظهر
**الحل:**
- تأكد من صحة تنسيق ملف `poems.txt`
- تأكد أن اسم الملف `poems.txt` وليس `poems.txt.txt`
- افتح Console في المتصفح (F12) وتحقق من الأخطاء

### المشكلة: الخطوط لا تظهر بشكل صحيح
**الحل:**
- تأكد من اتصالك بالإنترنت (الخطوط محمّلة من Google Fonts)
- امسح ذاكرة التخزين المؤقت للمتصفح

### المشكلة: الموقع لا يعمل على GitHub Pages
**الحل:**
- تأكد أن الملفات في المجلد الرئيسي وليس في مجلد فرعي
- تأكد أن اسم الملف الرئيسي `index.html` وليس `Index.html`
- انتظر 2-5 دقائق بعد الرفع

### المشكلة: الوضع الليلي لا يعمل
**الحل:**
- امسح Local Storage في المتصفح
- تأكد أن JavaScript مفعّل في المتصفح

---

## 📞 الدعم الفني

إذا واجهت أي مشكلة:

1. **تحقق من Console**: اضغط F12 في المتصفح → Console
2. **راجع الوثائق**: اقرأ `README.md`
3. **افتح Issue**: في GitHub إذا كانت المشكلة تقنية
4. **اتصل بنا**: عبر البريد الإلكتروني

---

## ✅ قائمة المراجعة النهائية

قبل النشر، تأكد من:

- [ ] جميع الملفات موجودة ومرفوعة
- [ ] ملف `poems.txt` منسّق بشكل صحيح
- [ ] تم تعديل الروابط في `sitemap.xml` و `robots.txt`
- [ ] الموقع يعمل بشكل صحيح محلياً
- [ ] تم اختبار جميع الصفحات
- [ ] الوضع الليلي يعمل
- [ ] البحث يعمل
- [ ] المفضلة تعمل
- [ ] الموقع متجاوب على الجوال

---

**🎉 تهانينا! موقعك جاهز للنشر**

للمساعدة: info@hamedghafri.com